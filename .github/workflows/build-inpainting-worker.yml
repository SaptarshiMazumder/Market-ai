name: Build Inpainting Worker

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Docker image tag (e.g. v1)"
        required: true
        default: "latest"

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: ./microservices/inpainting
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/flux-tok-inpainting-worker:${{ github.event.inputs.tag }}
